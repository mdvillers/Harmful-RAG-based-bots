# Example environment variables for Harmful-RAG-based-bots
# Copy this file to `.env` and fill in real values before running.

# Google Cloud project and locations
PROJECT_ID=your-gcp-project-id
# Default model location (will be used if a per-model location is not set)
DEFAULT_MODEL_LOCATION=us-central1
# Region / location for Discovery Engine (search data store)
REGION=us
DATA_STORE_ID=your-discovery-data-store-id

# --- Per-model MaaS IDs and optional per-model locations ---
# Provide the MaaS model name/identifier for each model you want to use.
# Example format: projects/<project>/locations/<location>/models/<model-id>

# LLaMA 4 (example key: "llama4")
LLAMA4_MODEL_NAME=meta/llama-4-maverick-17b-128e-instruct-maas
LLAMA4_MODEL_LOCATION=us-east5

# Gemini 2.5 Flash (example key: "gemini_2.5_flash")
GEMINI_2_5_FLASH_MODEL_NAME=google/gemini-2.5-flash-lite
GEMINI_2_5_FLASH_MODEL_LOCATION=us-central1

# Deepseek v3.1 (example key: "deepseek_3.1")
DEEPSEEK_3_1_MODEL_NAME=deepseek-ai/deepseek-v3.1-maas
DEEPSEEK_3_1_MODEL_LOCATION=us-west2

# Notes:
# - The script expects model keys like: llama4, mistral_small, gemini_25_flash, deepseek_31
#   When you pass `--model llama4` it will look for `LLAMA4_MODEL_ID` and optionally
#   `LLAMA4_MODEL_LOCATION`.
# - Keep your real `.env` file out of version control. This repository provides
#   `.env.example` only as a template.
